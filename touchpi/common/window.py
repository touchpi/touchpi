# The only global variable. Apps can run in an own window or in the desktop window.
# if this package variable is set in the Desktop class then the app does not need to initiate an own window.
os_window = None
